export class UpdatedDevice {

    deviceId: number;
    deviceNumber: string;

    constructor(deviceId: number,deviceNumber: string) {
        this.deviceId = deviceId;
        this.deviceNumber = deviceNumber;
    }
}
